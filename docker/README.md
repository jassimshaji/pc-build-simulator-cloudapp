# docker

`docker-compose.yml` for a local PostgreSQL instance (dev convenience only — the
deployed database is Neon, not this container). Will be added in Phase 1, Milestone 2
alongside the Prisma schema.
