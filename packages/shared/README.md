# packages/shared

Cross-cutting utilities used by multiple packages/apps.

**Status:** `apiSuccess`/`apiError`/`ApiResponse<T>` (`src/apiResponse.ts`) — the
`{ data, error }` envelope every `apps/web` API route returns, added in Phase 1,
Milestone 5. Formatting utilities/shared constants will be added as later phases
need them.
