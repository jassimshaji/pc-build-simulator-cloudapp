# packages/shared

Cross-cutting utilities used by multiple packages/apps: API response envelope helper,
formatting utilities, shared constants.

**Status:** package stub only (`@pcbuilder/shared`, empty `src/index.ts`) — will grow
organically as Phase 1+ code needs it.
